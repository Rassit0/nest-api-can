import { I18nValidationFilter } from './i18n-validation.filter';

describe('I18nValidationFilter', () => {
  it('should be defined', () => {
    expect(new I18nValidationFilter()).toBeDefined();
  });
});
